interface:
  display_name: "Wide-Lens Engineering"
  short_description: "Practical-first coding with optional assured delivery"
  default_prompt: "Use $wide-lens-engineering to select assurance, depth, and coordination separately; default to practical focused work only for isolated low-risk changes, escalate to assured when trust or blast radius requires it, let the active main model choose shared-subagent identities, count, and lanes, keep subagents read-only, and keep the main thread as the only writer."
